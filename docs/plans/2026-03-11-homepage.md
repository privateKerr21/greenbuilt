# Green Built Homepage Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build the Green Built homepage as a static HTML/CSS/JS page with a photo carousel hero, stat callouts, and alternating service card rows.

**Architecture:** Single `index.html` at the project root, shared `styles.css` and `nav.js` used by all pages. Assets (logos, photos) live in `assets/images/`. No build tools — everything is plain HTML/CSS/JS served directly by Vercel.

**Tech Stack:** HTML5, CSS3 (custom properties), vanilla JS, Google Fonts (Space Grotesk, Inter, Space Mono), Vercel static hosting.

**Spec:** `docs/specs/2026-03-11-homepage-design.md`
**Note on testing:** This is a static HTML site — there is no automated test framework. Every "verify" step means open the file in a browser (or use Live Server in VS Code) and visually confirm the described behavior.
**Prerequisite:** A git repo must already be initialized in the project root with a remote configured. If starting fresh: `git init && git remote add origin <repo-url>`. Vercel should be connected to the repo's `main` branch via the Vercel dashboard.

---

## Chunk 1: Assets + CSS Foundation

### Task 1: Copy and rename assets into project

**Files:**
- Create: `assets/images/` (directory)
- Create: `assets/images/logo-stacked.png`
- Create: `assets/images/logo-horizontal.png`
- Create: `assets/images/hero-01.jpg` through `hero-08.jpg`

- [ ] **Step 1: Create the assets directory**

```bash
mkdir -p assets/images
```

- [ ] **Step 2: Copy and rename logo files**

```bash
cp "../Branding/189654 GBC Logo-01.png" assets/images/logo-stacked.png
cp "../Branding/189654 GBC Logo-02.png" assets/images/logo-horizontal.png
```

- [ ] **Step 3: Copy and rename site photos**

```bash
cp "../Site Images/P_20231213_103539.jpg" assets/images/hero-01.jpg
cp "../Site Images/P_20231213_103830.jpg" assets/images/hero-02.jpg
cp "../Site Images/P_20240208_152339.jpg" assets/images/hero-03.jpg
cp "../Site Images/P_20240424_153121.jpg" assets/images/hero-04.jpg
cp "../Site Images/P_20250527_125723.jpg" assets/images/hero-05.jpg
cp "../Site Images/P_20250529_124205.jpg" assets/images/hero-06.jpg
cp "../Site Images/P_20250529_143750.jpg" assets/images/hero-07.jpg
cp "../Site Images/P_20250814_124945.jpg" assets/images/hero-08.jpg
```

- [ ] **Step 4: Verify all 10 files exist**

```bash
ls assets/images/
```
Expected output: `hero-01.jpg hero-02.jpg hero-03.jpg hero-04.jpg hero-05.jpg hero-06.jpg hero-07.jpg hero-08.jpg logo-horizontal.png logo-stacked.png`

- [ ] **Step 5: Commit**

```bash
git add assets/
git commit -m "feat: add brand assets (logos + site photos)"
```

---

### Task 2: Create shared CSS design system

**Files:**
- Create: `styles.css`

- [ ] **Step 1: Create `styles.css` with CSS custom properties, reset, and base typography**

```css
/* ============================================================
   GREEN BUILT CONSTRUCTION — Shared Styles
   ============================================================ */

/* Note: Google Fonts are loaded via <link> tags in each page's <head>, not via @import here.
   See index.html <head> for the preconnect + stylesheet links. */

/* ---- Custom Properties ---- */
:root {
  --green:        #6DC033;
  --green-dark:   #2D5A1B;
  --green-hover:  #5aaa27;
  --blue:         #4A7DB5;
  --blue-hover:   #3a6a9e;
  --bg:           #F8FAF6;
  --white:        #FFFFFF;
  --ink:          #1A1A1A;
  --muted:        #6B7280;
  --border:       #D1D5DB;

  --font-head:    'Space Grotesk', sans-serif;
  --font-body:    'Inter', sans-serif;
  --font-mono:    'Space Mono', monospace;

  --radius:       8px;
  --max-width:    1120px;
  --section-pad:  80px;
}

/* ---- Reset ---- */
*, *::before, *::after {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

html { scroll-behavior: smooth; }

body {
  font-family: var(--font-body);
  font-size: 1rem;
  line-height: 1.6;
  color: var(--ink);
  background: var(--bg);
}

img {
  display: block;
  max-width: 100%;
}

a {
  color: inherit;
  text-decoration: none;
}

/* ---- Layout ---- */
.container {
  width: 100%;
  max-width: var(--max-width);
  margin: 0 auto;
  padding: 0 24px;
}

/* ---- Typography ---- */
h1, h2, h3, h4 {
  font-family: var(--font-head);
  font-weight: 700;
  line-height: 1.15;
}

h1 { font-size: clamp(2.2rem, 5vw, 3.8rem); }
h2 { font-size: clamp(1.8rem, 3.5vw, 2.8rem); }
h3 { font-size: clamp(1.3rem, 2.5vw, 1.8rem); }

.label {
  font-family: var(--font-mono);
  font-size: 0.75rem;
  font-weight: 700;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  color: var(--muted);
}

/* ---- Buttons ---- */
.btn {
  display: inline-block;
  padding: 14px 28px;
  font-family: var(--font-head);
  font-size: 0.95rem;
  font-weight: 700;
  border-radius: var(--radius);
  cursor: pointer;
  transition: background 0.2s, color 0.2s, border-color 0.2s, transform 0.15s;
  border: 2px solid transparent;
  white-space: nowrap;
}

.btn:hover { transform: translateY(-1px); }

.btn-green {
  background: var(--green);
  color: var(--white);
  border-color: var(--green);
}
.btn-green:hover {
  background: var(--green-hover);
  border-color: var(--green-hover);
}

.btn-blue-outline {
  background: transparent;
  color: var(--blue);
  border-color: var(--blue);
}
.btn-blue-outline:hover {
  background: var(--blue);
  color: var(--white);
}

.btn-white-outline {
  background: transparent;
  color: var(--white);
  border-color: var(--white);
}
.btn-white-outline:hover {
  background: var(--white);
  color: var(--green-dark);
}

/* ---- Section base ---- */
.section {
  padding: var(--section-pad) 0;
}

/* ---- Nav ---- */
.nav {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  z-index: 100;
  background: var(--white);
  border-bottom: 1px solid var(--border);
  height: 72px;
  display: flex;
  align-items: center;
}

.nav-inner {
  display: flex;
  align-items: center;
  justify-content: space-between;
  width: 100%;
  max-width: var(--max-width);
  margin: 0 auto;
  padding: 0 24px;
  gap: 24px;
}

.nav-logo img {
  height: 44px;
  width: auto;
}

.nav-links {
  display: flex;
  align-items: center;
  gap: 32px;
  list-style: none;
  font-family: var(--font-head);
  font-size: 0.9rem;
  font-weight: 500;
}

.nav-links a {
  color: var(--ink);
  transition: color 0.2s;
}
.nav-links a:hover { color: var(--green); }

.nav-ctas {
  display: flex;
  align-items: center;
  gap: 12px;
  flex-shrink: 0;
}

.nav-ctas .btn {
  padding: 10px 20px;
  font-size: 0.88rem;
}

/* Mobile nav */
.nav-toggle {
  display: none;
  flex-direction: column;
  gap: 5px;
  background: none;
  border: none;
  cursor: pointer;
  padding: 4px;
}
.nav-toggle span {
  display: block;
  width: 24px;
  height: 2px;
  background: var(--ink);
  transition: transform 0.25s, opacity 0.25s;
}

.nav-mobile {
  display: none;
  position: fixed;
  top: 72px;
  left: 0;
  right: 0;
  bottom: 0;
  background: var(--white);
  z-index: 99;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 32px;
}
.nav-mobile.open { display: flex; }
.nav-mobile a {
  font-family: var(--font-head);
  font-size: 1.5rem;
  font-weight: 700;
  color: var(--ink);
}
.nav-mobile a:hover { color: var(--green); }
.nav-mobile .btn { font-size: 1rem; }

/* ---- Footer ---- */
.footer {
  background: var(--ink);
  color: var(--white);
  padding: 60px 0 32px;
}

.footer-grid {
  display: grid;
  grid-template-columns: 1.5fr 1fr 1fr;
  gap: 48px;
  margin-bottom: 48px;
}

.footer-logo img {
  height: 48px;
  width: auto;
  filter: brightness(0) invert(1);
  margin-bottom: 16px;
}

.footer-tagline {
  font-family: var(--font-head);
  font-size: 0.95rem;
  color: #9CA3AF;
  margin-bottom: 12px;
}

.footer-area {
  font-size: 0.85rem;
  color: #9CA3AF;
}

.footer-col h4 {
  font-family: var(--font-head);
  font-size: 0.85rem;
  font-weight: 700;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: #9CA3AF;
  margin-bottom: 16px;
}

.footer-col ul {
  list-style: none;
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.footer-col ul a {
  font-size: 0.9rem;
  color: #D1D5DB;
  transition: color 0.2s;
}
.footer-col ul a:hover { color: var(--green); }

.footer-col address {
  font-style: normal;
  font-size: 0.9rem;
  color: #D1D5DB;
  line-height: 1.8;
}

.footer-bottom {
  border-top: 1px solid #374151;
  padding-top: 24px;
  text-align: center;
  font-size: 0.8rem;
  color: #6B7280;
}

/* ---- Responsive ---- */
@media (max-width: 768px) {
  .nav-links,
  .nav-ctas { display: none; }
  .nav-toggle { display: flex; }

  .footer-grid {
    grid-template-columns: 1fr;
    gap: 32px;
  }
}
```

- [ ] **Step 2: Open a blank HTML file in a browser to verify fonts load**

Create a quick test file `test.html` at the project root:
```html
<!DOCTYPE html>
<html><head>
  <link rel="stylesheet" href="styles.css">
</head><body style="padding:40px">
  <h1>Space Grotesk Headline</h1>
  <p class="label">Space Mono Label</p>
  <p>Inter body text</p>
  <a href="#" class="btn btn-green">Green Button</a>
  <a href="#" class="btn btn-blue-outline">Blue Outline</a>
</body></html>
```
Open in browser. Expected: three distinct fonts, green and blue-outline buttons render correctly.

- [ ] **Step 3: Delete `test.html`**

```bash
rm test.html
```

- [ ] **Step 4: Commit**

```bash
git add styles.css
git commit -m "feat: add shared CSS design system"
```

---

### Task 3: Create shared nav JS

**Files:**
- Create: `nav.js`

- [ ] **Step 1: Create `nav.js`**

```js
// nav.js — mobile menu toggle
(function () {
  const toggle = document.querySelector('.nav-toggle');
  const mobileNav = document.querySelector('.nav-mobile');
  if (!toggle || !mobileNav) return;

  toggle.addEventListener('click', function () {
    const isOpen = mobileNav.classList.toggle('open');
    toggle.setAttribute('aria-expanded', isOpen);
    mobileNav.setAttribute('aria-hidden', !isOpen);
    document.body.style.overflow = isOpen ? 'hidden' : '';
  });

  // Close on link click
  mobileNav.querySelectorAll('a').forEach(function (link) {
    link.addEventListener('click', function () {
      mobileNav.classList.remove('open');
      toggle.setAttribute('aria-expanded', false);
      mobileNav.setAttribute('aria-hidden', true);
      document.body.style.overflow = '';
    });
  });
})();
```

- [ ] **Step 2: Commit**

```bash
git add nav.js
git commit -m "feat: add shared mobile nav JS"
```

---

## Chunk 2: Homepage HTML

### Task 4: Build `index.html` — full homepage

**Files:**
- Create: `index.html`

This task builds the complete homepage HTML in one pass. The carousel JS is wired up in Task 5.

- [ ] **Step 1: Create `index.html`**

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Green Built Construction — Your Green Comfort Solution</title>
  <meta name="description" content="Geothermal and traditional HVAC serving the Intermountain West. Green Built Construction installs, designs, and drills geothermal systems that cut energy bills by up to 70%.">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500&family=Space+Grotesk:wght@500;700;800&family=Space+Mono:wght@400;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="styles.css">
  <style>
    /* ============================================================
       Homepage-specific styles
       ============================================================ */

    /* ---- Hero ---- */
    .hero {
      position: relative;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      overflow: hidden;
      background: var(--bg);
    }

    /* Carousel slides */
    .hero-carousel {
      position: absolute;
      inset: 0;
      z-index: 0;
    }

    .hero-slide {
      position: absolute;
      inset: 0;
      background-size: cover;
      background-position: center;
      opacity: 0;
      transition: opacity 0.8s ease-in-out;
    }

    .hero-slide.active { opacity: 0.55; }

    /* Radial glow overlay */
    .hero-glow {
      position: absolute;
      inset: 0;
      z-index: 1;
      background: radial-gradient(
        ellipse 55% 65% at 50% 50%,
        rgba(248, 250, 246, 1) 0%,
        rgba(248, 250, 246, 0.7) 35%,
        rgba(248, 250, 246, 0) 100%
      );
    }

    /* Hero content */
    .hero-content {
      position: relative;
      z-index: 2;
      display: flex;
      flex-direction: column;
      align-items: center;
      text-align: center;
      padding: 120px 24px 80px;
    }

    .hero-logo {
      height: clamp(120px, 16vw, 180px);
      width: auto;
      margin-bottom: 20px;
    }

    .hero-tagline {
      font-family: var(--font-head);
      font-size: clamp(1.1rem, 2.5vw, 1.4rem);
      font-weight: 500;
      color: var(--blue);
      letter-spacing: 0.04em;
      margin-bottom: 36px;
    }

    .hero-ctas {
      display: flex;
      gap: 16px;
      flex-wrap: wrap;
      justify-content: center;
    }

    /* ---- The Problem ---- */
    .section-problem {
      background: var(--bg);
      text-align: center;
    }

    .section-problem .label {
      margin-bottom: 16px;
    }

    .section-problem h2 {
      max-width: 640px;
      margin: 0 auto 24px;
    }

    .section-problem p {
      max-width: 620px;
      margin: 0 auto;
      color: var(--muted);
      font-size: 1.05rem;
      line-height: 1.75;
    }

    /* ---- Why Geothermal (Stats) ---- */
    .section-stats {
      background: var(--white);
    }

    .stats-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 0;
      border: 1px solid var(--border);
      border-radius: var(--radius);
      overflow: hidden;
    }

    .stat-item {
      padding: 48px 32px;
      text-align: center;
      border-right: 1px solid var(--border);
    }

    .stat-item:last-child { border-right: none; }

    .stat-value {
      font-family: var(--font-head);
      font-size: clamp(2rem, 4vw, 3rem);
      font-weight: 800;
      color: var(--green);
      line-height: 1;
      margin-bottom: 12px;
    }

    .stat-label {
      font-size: 0.95rem;
      color: var(--muted);
      line-height: 1.5;
    }

    /* ---- Contractor Callout ---- */
    .section-contractor {
      background: var(--green-dark);
      color: var(--white);
      text-align: center;
    }

    .section-contractor h2 {
      color: var(--white);
      max-width: 640px;
      margin: 0 auto 16px;
    }

    .section-contractor p {
      color: rgba(255,255,255,0.75);
      font-size: 1.05rem;
      max-width: 480px;
      margin: 0 auto 36px;
    }

    /* ---- Service Cards (alternating) ---- */
    .section-services {
      background: var(--bg);
    }

    .section-services .section-header {
      text-align: center;
      margin-bottom: 64px;
    }

    .section-services .section-header h2 {
      margin-bottom: 12px;
    }

    .section-services .section-header p {
      color: var(--muted);
      max-width: 480px;
      margin: 0 auto;
    }

    .service-row {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 64px;
      align-items: center;
      margin-bottom: 80px;
    }

    .service-row:last-child { margin-bottom: 0; }

    /* Even rows: image left, text right */
    .service-row.reverse .service-text { order: 2; }
    .service-row.reverse .service-image { order: 1; }

    .service-image img {
      width: 100%;
      height: 360px;
      object-fit: cover;
      border-radius: var(--radius);
    }

    .service-text h3 {
      color: var(--green);
      margin-bottom: 16px;
    }

    .service-text p {
      color: var(--muted);
      font-size: 1rem;
      line-height: 1.75;
      margin-bottom: 28px;
    }

    /* ---- Final CTA ---- */
    .section-cta {
      background: var(--white);
      text-align: center;
    }

    .section-cta h2 {
      margin-bottom: 16px;
    }

    .section-cta p {
      color: var(--muted);
      font-size: 1.05rem;
      max-width: 440px;
      margin: 0 auto 36px;
    }

    /* ---- Mobile ---- */
    @media (max-width: 768px) {
      .stats-grid {
        grid-template-columns: 1fr;
      }
      .stat-item {
        border-right: none;
        border-bottom: 1px solid var(--border);
      }
      .stat-item:last-child { border-bottom: none; }

      .service-row,
      .service-row.reverse {
        grid-template-columns: 1fr;
        gap: 28px;
      }
      /* Force image above text on ALL rows (both regular and reverse) */
      .service-row .service-image,
      .service-row.reverse .service-image { order: 1; }
      .service-row .service-text,
      .service-row.reverse .service-text { order: 2; }

      .service-image img { height: 240px; }
    }
  </style>
</head>
<body>

  <!-- ========== NAV ========== -->
  <nav class="nav" aria-label="Main navigation">
    <div class="nav-inner">
      <a href="/" class="nav-logo" aria-label="Green Built Construction home">
        <img src="assets/images/logo-horizontal.png" alt="Green Built Construction">
      </a>
      <ul class="nav-links">
        <li><a href="/geothermal">Geothermal</a></li>
        <li><a href="/traditional">Traditional HVAC</a></li>
        <li><a href="/drilling">Drilling</a></li>
        <li><a href="/design-services">Design Services</a></li>
        <li><a href="/contractors">Contractors</a></li>
      </ul>
      <div class="nav-ctas">
        <a href="/start" class="btn btn-green">Start a Project</a>
        <a href="/contractors" class="btn btn-blue-outline">Work With Us</a>
      </div>
      <button class="nav-toggle" aria-label="Open menu" aria-expanded="false">
        <span></span><span></span><span></span>
      </button>
    </div>
  </nav>

  <!-- Mobile nav overlay -->
  <div class="nav-mobile" aria-hidden="true">
    <a href="/geothermal">Geothermal</a>
    <a href="/traditional">Traditional HVAC</a>
    <a href="/drilling">Drilling</a>
    <a href="/design-services">Design Services</a>
    <a href="/contractors">Contractors</a>
    <a href="/start" class="btn btn-green">Start a Project</a>
    <a href="/contractors" class="btn btn-blue-outline">Work With Us</a>
  </div>

  <!-- ========== HERO ========== -->
  <section class="hero" aria-label="Hero">

    <div class="hero-carousel" aria-hidden="true">
      <div class="hero-slide active" style="background-image: url('assets/images/hero-01.jpg')"></div>
      <div class="hero-slide" style="background-image: url('assets/images/hero-02.jpg')"></div>
      <div class="hero-slide" style="background-image: url('assets/images/hero-03.jpg')"></div>
      <div class="hero-slide" style="background-image: url('assets/images/hero-04.jpg')"></div>
      <div class="hero-slide" style="background-image: url('assets/images/hero-05.jpg')"></div>
      <div class="hero-slide" style="background-image: url('assets/images/hero-06.jpg')"></div>
      <div class="hero-slide" style="background-image: url('assets/images/hero-07.jpg')"></div>
      <div class="hero-slide" style="background-image: url('assets/images/hero-08.jpg')"></div>
    </div>

    <div class="hero-glow" aria-hidden="true"></div>

    <div class="hero-content">
      <img
        src="assets/images/logo-stacked.png"
        alt="Green Built Construction"
        class="hero-logo"
      >
      <p class="hero-tagline">Your Green Comfort Solution</p>
      <div class="hero-ctas">
        <a href="/start" class="btn btn-green">Get a Quote</a>
        <a href="/contractors" class="btn btn-blue-outline">Work With Us</a>
      </div>
    </div>

  </section>

  <!-- ========== THE PROBLEM ========== -->
  <section class="section section-problem">
    <div class="container">
      <span class="label">The Smarter Choice</span>
      <h2>Traditional HVAC is cheap to install.<br>Expensive to own.</h2>
      <p>Traditional systems cost less upfront but run on electricity or gas at full price forever. Geothermal flips the equation — higher install cost, dramatically lower operating costs, and a system that lasts 25+ years. The math favors geothermal.</p>
    </div>
  </section>

  <!-- ========== WHY GEOTHERMAL (STATS) ========== -->
  <section class="section section-stats">
    <div class="container">
      <div class="stats-grid">
        <div class="stat-item">
          <div class="stat-value">2–3×</div>
          <div class="stat-label">more energy efficient<br>than traditional systems</div>
        </div>
        <div class="stat-item">
          <div class="stat-value">$370–$500+</div>
          <div class="stat-label">average annual energy savings<br>for homeowners</div>
        </div>
        <div class="stat-item">
          <div class="stat-value">30%</div>
          <div class="stat-label">federal tax credit<br>on qualifying installations</div>
        </div>
      </div>
    </div>
  </section>

  <!-- ========== CONTRACTOR CALLOUT ========== -->
  <section class="section section-contractor">
    <div class="container">
      <h2>We work with HVAC contractors across the Intermountain West.</h2>
      <p>Referral partnerships and subcontract opportunities available.</p>
      <a href="/contractors" class="btn btn-white-outline">See Contractor Opportunities</a>
    </div>
  </section>

  <!-- ========== SERVICE CARDS ========== -->
  <section class="section section-services">
    <div class="container">

      <div class="section-header">
        <span class="label">What We Do</span>
        <h2>Explore Our Services</h2>
        <p>From geothermal ground loops to traditional installs — we design, drill, and build it all.</p>
      </div>

      <!-- Row 1: Text left, image right -->
      <div class="service-row">
        <div class="service-text">
          <h3>Geothermal HVAC</h3>
          <p>The earth beneath your home stays a constant 50–55°F year-round. Geothermal systems tap into that free energy to heat and cool your home at 2–3× the efficiency of traditional HVAC — with no combustion, no gas bill, and a lifespan that outlasts every other system on the market.</p>
          <a href="/geothermal" class="btn btn-green">Learn More</a>
        </div>
        <div class="service-image">
          <img src="assets/images/hero-05.jpg" alt="Geothermal HVAC installation">
        </div>
      </div>

      <!-- Row 2: Image left, text right -->
      <div class="service-row reverse">
        <div class="service-text">
          <h3>Traditional HVAC</h3>
          <p>Sometimes geothermal isn't the right fit — and that's okay. We install and service high-efficiency traditional HVAC systems too. We'll give you an honest comparison and recommend what actually makes sense for your home, budget, and timeline.</p>
          <a href="/traditional" class="btn btn-green">Learn More</a>
        </div>
        <div class="service-image">
          <img src="assets/images/hero-01.jpg" alt="Traditional HVAC installation">
        </div>
      </div>

      <!-- Row 3: Text left, image right -->
      <div class="service-row">
        <div class="service-text">
          <h3>Drilling</h3>
          <p>Most geothermal companies sub out their drilling. We don't. Our in-house drilling crew handles every borehole and ground loop installation — which means better coordination, tighter quality control, and lower cost passed on to you.</p>
          <a href="/drilling" class="btn btn-green">Learn More</a>
        </div>
        <div class="service-image">
          <img src="assets/images/hero-03.jpg" alt="Geothermal drilling operation">
        </div>
      </div>

      <!-- Row 4: Image left, text right -->
      <div class="service-row reverse">
        <div class="service-text">
          <h3>Design Services</h3>
          <p>The right system starts with the right design. We perform full Manual J load calculations, system sizing, and site assessments before any equipment is specified — ensuring your system performs exactly as designed for decades.</p>
          <a href="/design-services" class="btn btn-green">Learn More</a>
        </div>
        <div class="service-image">
          <img src="assets/images/hero-07.jpg" alt="HVAC system design and planning">
        </div>
      </div>

    </div>
  </section>

  <!-- ========== FINAL CTA ========== -->
  <section class="section section-cta">
    <div class="container">
      <h2>Ready to get started?</h2>
      <p>Tell us about your project. We'll review it and reach out within 1 business day.</p>
      <a href="/start" class="btn btn-green">Start a Project</a>
    </div>
  </section>

  <!-- ========== FOOTER ========== -->
  <footer class="footer">
    <div class="container">
      <div class="footer-grid">

        <div>
          <div class="footer-logo">
            <img src="assets/images/logo-horizontal.png" alt="Green Built Construction">
          </div>
          <p class="footer-tagline">Your Green Comfort Solution</p>
          <p class="footer-area">Serving Utah, Idaho, Nevada, Colorado, and Wyoming.</p>
        </div>

        <div class="footer-col">
          <h4>Services</h4>
          <ul>
            <li><a href="/geothermal">Geothermal HVAC</a></li>
            <li><a href="/traditional">Traditional HVAC</a></li>
            <li><a href="/drilling">Drilling</a></li>
            <li><a href="/design-services">Design Services</a></li>
            <li><a href="/contractors">Work With Us</a></li>
            <li><a href="/start">Start a Project</a></li>
          </ul>
        </div>

        <div class="footer-col">
          <h4>Contact</h4>
          <address>
            Phone: (000) 000-0000<br>
            Email: info@greenbuilt.com<br>
            <br>
            Intermountain West<br>
            UT · ID · NV · CO · WY
          </address>
        </div>

      </div>

      <div class="footer-bottom">
        <p>&copy; 2026 Green Built Construction. All rights reserved.</p>
      </div>
    </div>
  </footer>

  <script src="nav.js"></script>
  <script src="carousel.js"></script>
</body>
</html>
```

- [ ] **Step 2: Open `index.html` in browser and verify structure**

Expected:
- Nav appears at top with logo, links, and two CTA buttons
- Hero section fills viewport with logo, tagline, and two CTAs visible
- All 5 content sections appear below the hero
- Footer renders with 3 columns
- No broken image icons (logos load from `assets/images/`)
- No console errors

- [ ] **Step 3: Commit**

```bash
git add index.html
git commit -m "feat: add homepage HTML structure"
```

---

## Chunk 3: Hero Carousel JS

### Task 5: Build the hero photo carousel

**Files:**
- Create: `carousel.js`

- [ ] **Step 1: Create `carousel.js`**

```js
// carousel.js — hero background photo carousel
(function () {
  var slides = document.querySelectorAll('.hero-slide');
  if (!slides.length) return;

  var current = 0;
  var interval = 5000;   // ms between slides
  var duration = 800;    // matches CSS transition

  function advance() {
    slides[current].classList.remove('active');
    current = (current + 1) % slides.length;
    slides[current].classList.add('active');
  }

  // Start after first interval
  setInterval(advance, interval);
})();
```

- [ ] **Step 2: Open `index.html` in browser and verify carousel**

Expected:
- Background photo changes every 5 seconds
- Transition is a smooth crossfade (not a hard cut)
- First slide is visible immediately on load
- Logo, tagline, and CTAs remain visible throughout

- [ ] **Step 3: Commit**

```bash
git add carousel.js
git commit -m "feat: add hero photo carousel"
```

---

## Chunk 4: Final Verification + Contact Placeholder Update

### Task 6: Update footer contact placeholders and verify full page

**Files:**
- Modify: `index.html` (footer contact section)

- [ ] **Step 1: Confirm contact details with client**

The footer currently has placeholder phone/email. Before going live, replace:
- `(000) 000-0000` → actual phone
- `info@greenbuilt.com` → actual email

If details are not yet available, leave placeholders and track as a blocker in `tasks.md`.

- [ ] **Step 2: Full page visual QA checklist**

Open `index.html` in browser and verify each section:

| Section | Check |
|---|---|
| Nav | Logo loads, links visible, sticky on scroll |
| Hero | Carousel cycles, logo + tagline + CTAs centered |
| The Problem | Text readable, good spacing |
| Stats | 3 columns, green numbers, no overflow |
| Contractor callout | Dark green background, white text |
| Service rows 1–4 | Alternating layout, all 4 photos load, green headings |
| Final CTA | Centered, green button |
| Footer | 3-column grid, logo inverted white, links present |

- [ ] **Step 3: Mobile QA (resize browser to 375px wide)**

| Element | Check |
|---|---|
| Nav | Hamburger visible, links hidden |
| Hamburger tap | Mobile overlay opens, links present |
| Hero | Logo + tagline + CTAs readable |
| Stats | Single column stack |
| Service rows | Single column, image above text for all 4 |
| Footer | Single column stack |

- [ ] **Step 4: Update tasks.md to reflect homepage complete**

Mark the following tasks complete in `tasks.md`:
- `[x]` Create folder structure
- `[x]` Create shared `styles.css`
- `[x]` Create shared `nav.js`
- `[x]` Build `index.html`

Add to Blocked section:
- `[ ]` Update footer contact info — **Blocker**: need phone + email from client
- `[ ]` Configure Formspree endpoint — **Blocker**: need client email for form routing

- [ ] **Step 5: Final commit**

```bash
git add index.html tasks.md
git commit -m "feat: complete homepage — hero carousel, all sections, mobile responsive"
```

---

## Summary

| Task | Files Created/Modified |
|---|---|
| 1 | `assets/images/` (10 files) |
| 2 | `styles.css` |
| 3 | `nav.js` |
| 4 | `index.html` |
| 5 | `carousel.js` |
| 6 | `index.html` (QA + contact), `tasks.md` |

**After homepage is complete:** Proceed to Phase 2 — service pages (`geothermal/`, `traditional/`, `drilling/`, `design-services/`) following the same shared `styles.css` + nav pattern.

**Open blockers (do not mark homepage "done" until tracked):**
- Client phone + email for footer
- Formspree endpoint for `/start` form
